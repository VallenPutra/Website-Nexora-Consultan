<x-admin.layout title="Insight Details"><div class="mx-auto max-w-4xl space-y-6"><div class="flex flex-wrap items-end justify-between gap-4"><div><a href="{{ route('admin.insights.index') }}" class="text-sm font-medium text-muted hover:text-accent">&larr; Back to Insights</a><h1 class="mt-3 text-2xl font-bold text-navy">{{ $insight->title }}</h1><p class="mt-1 text-sm text-muted">{{ $insight->category }} &middot; {{ $insight->author }}</p></div><div class="flex gap-3"><a href="{{ route('admin.insights.edit', $insight) }}" class="admin-btn-primary">Edit Insight</a><form method="POST" action="{{ route('admin.insights.destroy', $insight) }}" onsubmit="return confirm('Delete this insight?')">@csrf @method('DELETE')<button type="submit" class="admin-btn-secondary text-red-600 hover:border-red-300 hover:text-red-700">Delete</button></form></div></div>@if (session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<div class="admin-card p-5"><div class="flex items-center justify-between gap-4"><div><p class="text-xs uppercase tracking-wide text-muted">{{ $insight->slug }}</p><p class="mt-2 text-sm text-muted">{{ $insight->published_at?->format('d F Y') ?: 'No publication date' }}</p></div><span class="admin-badge {{ $insight->is_published ? 'admin-badge-success' : 'admin-badge-neutral' }}">{{ $insight->is_published ? 'Published' : 'Draft' }}</span></div><p class="mt-6 text-base font-medium leading-relaxed text-navy">{{ $insight->excerpt }}</p><div class="mt-6 border-t border-navy/10 pt-6"><p class="whitespace-pre-line text-sm leading-relaxed text-charcoal sm:text-base">{{ $insight->body ?: 'No body content has been added.' }}</p></div></div></div></x-admin.layout>
