<x-admin.layout title="Service Details"><div class="mx-auto max-w-3xl space-y-6"><div class="flex flex-wrap items-end justify-between gap-4"><div><a href="{{ route('admin.services.index') }}" class="text-sm font-medium text-muted hover:text-accent">&larr; Back to Services</a><h1 class="mt-3 text-2xl font-bold text-navy">{{ $service->title }}</h1><p class="mt-1 text-sm text-muted">{{ $service->slug }}</p></div><div class="flex gap-3"><a href="{{ route('admin.services.edit', $service) }}" class="admin-btn-primary">Edit Service</a><form method="POST" action="{{ route('admin.services.destroy', $service) }}" onsubmit="return confirm('Delete this service?')">@csrf @method('DELETE')<button type="submit" class="admin-btn-secondary text-red-600 hover:border-red-300 hover:text-red-700">Delete</button></form></div></div><div class="admin-card p-5"><div class="flex items-center justify-between gap-4"><h2 class="text-base font-semibold text-navy">Public Catalog Entry</h2><span class="admin-badge {{ $service->is_active ? 'admin-badge-success' : 'admin-badge-neutral' }}">{{ $service->is_active ? 'Active' : 'Inactive' }}</span></div><p class="mt-5 text-sm leading-relaxed text-muted">{{ $service->short ?: 'No short description has been added.' }}</p><dl class="mt-6 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs uppercase tracking-wide text-muted">Slug</dt><dd class="mt-1 text-sm text-charcoal">{{ $service->slug }}</dd></div><div><dt class="text-xs uppercase tracking-wide text-muted">Display order</dt><dd class="mt-1 text-sm text-charcoal">{{ $service->sort_order }}</dd></div></dl></div></div></x-admin.layout>
