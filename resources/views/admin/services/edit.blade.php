<x-admin.layout title="Edit Service"><div class="mx-auto max-w-3xl space-y-6"><div><a href="{{ route('admin.services.index') }}" class="text-sm font-medium text-muted hover:text-accent">&larr; Back to Services</a><h1 class="mt-3 text-2xl font-bold text-navy">Edit Service</h1><p class="mt-1 text-sm text-muted">Update {{ $service->title }} and its public visibility.</p></div>@if (session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('admin.services.update', $service) }}" class="admin-card p-5 sm:p-6">@method('PUT')@include('admin.services._form', ['submitLabel' => 'Save Changes'])</form></div></x-admin.layout>
