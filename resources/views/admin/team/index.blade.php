<x-admin.layout title="Team"><div class="mx-auto max-w-7xl space-y-6"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-wide text-accent">Business Management</p><h1 class="mt-1 text-2xl font-bold text-navy">Team</h1><p class="mt-1 text-sm text-muted">Manage the team profiles shown on the public website.</p></div><a href="{{ route('admin.team.create') }}" class="admin-btn-primary"><x-admin.icon name="plus" class="h-4 w-4" /> Add Member</a></div>@if (session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<div class="admin-card overflow-hidden"><div class="border-b border-navy/10 p-4"><form method="GET" action="{{ route('admin.team.index') }}" class="flex max-w-md gap-2"><input type="search" name="q" value="{{ request('q') }}" placeholder="Search team..." class="min-w-0 flex-1 rounded-lg border border-navy/15 px-3.5 py-2.5 text-sm focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent"><button type="submit" class="admin-btn-secondary">Search</button></form></div>@if ($members->isEmpty())<div class="p-5"><x-admin.empty-state title="No team members yet" description="Add a team member to build the public team page." icon="team"><a href="{{ route('admin.team.create') }}" class="admin-btn-primary mt-6">Add First Member</a></x-admin.empty-state></div>@else<div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b border-navy/10 text-xs uppercase tracking-wide text-muted"><tr><th class="px-5 py-3 font-medium">Member</th><th class="px-5 py-3 font-medium">Role</th><th class="px-5 py-3 font-medium">Expertise</th><th class="px-5 py-3 font-medium">Status</th><th class="px-5 py-3 text-right font-medium">Action</th></tr></thead><tbody class="divide-y divide-navy/5">@foreach ($members as $member)<tr class="hover:bg-surface/70"><td class="px-5 py-4"><a href="{{ route('admin.team.show', $member) }}" class="font-semibold text-navy hover:text-accent">{{ $member->name }}</a><p class="mt-0.5 text-xs text-muted">Order {{ $member->sort_order }}</p></td><td class="px-5 py-4 text-muted">{{ $member->role }}</td><td class="px-5 py-4 text-muted">{{ $member->expertise ?: '—' }}</td><td class="px-5 py-4"><span class="admin-badge {{ $member->is_active ? 'admin-badge-success' : 'admin-badge-neutral' }}">{{ $member->is_active ? 'Active' : 'Inactive' }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('admin.team.edit', $member) }}" class="text-sm font-semibold text-accent hover:text-amber-600">Edit</a></td></tr>@endforeach</tbody></table></div><div class="border-t border-navy/10 px-5 py-4">{{ $members->links() }}</div>@endif</div></div></x-admin.layout>
