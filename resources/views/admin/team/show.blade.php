<x-admin.layout title="Team Member Details"><div class="mx-auto max-w-3xl space-y-6"><div class="flex flex-wrap items-end justify-between gap-4"><div><a href="{{ route('admin.team.index') }}" class="text-sm font-medium text-muted hover:text-accent">&larr; Back to Team</a><h1 class="mt-3 text-2xl font-bold text-navy">{{ $member->name }}</h1><p class="mt-1 text-sm text-muted">{{ $member->role }}</p></div><div class="flex gap-3"><a href="{{ route('admin.team.edit', $member) }}" class="admin-btn-primary">Edit Member</a><form method="POST" action="{{ route('admin.team.destroy', $member) }}" onsubmit="return confirm('Delete this team member?')">@csrf @method('DELETE')<button type="submit" class="admin-btn-secondary text-red-600 hover:border-red-300 hover:text-red-700">Delete</button></form></div></div><div class="admin-card p-5"><div class="flex items-center justify-between gap-4"><h2 class="text-base font-semibold text-navy">Public Profile</h2><span class="admin-badge {{ $member->is_active ? 'admin-badge-success' : 'admin-badge-neutral' }}">{{ $member->is_active ? 'Active' : 'Inactive' }}</span></div><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs uppercase tracking-wide text-muted">Role</dt><dd class="mt-1 text-sm text-charcoal">{{ $member->role }}</dd></div><div><dt class="text-xs uppercase tracking-wide text-muted">Expertise</dt><dd class="mt-1 text-sm text-charcoal">{{ $member->expertise ?: '—' }}</dd></div></dl><div class="mt-6 border-t border-navy/10 pt-5"><h3 class="text-sm font-semibold text-navy">Bio</h3><p class="mt-2 whitespace-pre-line text-sm leading-relaxed text-muted">{{ $member->bio ?: 'No bio has been added.' }}</p></div></div></div></x-admin.layout>
